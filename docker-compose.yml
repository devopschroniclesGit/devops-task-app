version: '3.8'

services:
  db:
    image: docker.io/library/postgres:13
    environment:
      POSTGRES_DB: taskdb
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
    ports:
      - "5433:5432"

  api:
    build: ./app
    ports:
      - "5000:5000"
    depends_on:
      - db
