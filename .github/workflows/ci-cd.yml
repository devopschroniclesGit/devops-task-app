name: Task API | Build, Push and Deploy

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'chapters/**'
      - '**/*.md'
  workflow_dispatch:

jobs:
  deploy-application:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: docker.io/${{ secrets.DOCKER_USERNAME }}/task-app:latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Authenticate with Docker Hub
        run: |
          docker logout || true
          echo "${{ secrets.DOCKER_PASSWORD }}" | \
          docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: docker build -t $IMAGE_NAME ./app

      - name: Push Docker image
        run: docker push $IMAGE_NAME

      - name: Validate deployment secrets
        run: |
          [ -z "${{ secrets.EC2_HOST }}" ] && echo "EC2_HOST missing" && exit 1
          [ -z "${{ secrets.EC2_USER }}" ] && echo "EC2_USER missing" && exit 1
          [ -z "${{ secrets.EC2_SSH_KEY }}" ] && echo "EC2_SSH_KEY missing" && exit 1
          echo "All deployment secrets validated"

      - name: Test EC2 connectivity
        run: |
          echo "Checking host..."
          ping -c 2 ${{ secrets.EC2_HOST }}

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            IMAGE_NAME="docker.io/${{ secrets.DOCKER_USERNAME }}/task-app:latest"

            echo "Connected successfully"
            hostname
            whoami
            docker --version

            docker pull $IMAGE_NAME

            docker stop task-app || true
            docker rm task-app || true

            docker run -d \
              --restart unless-stopped \
              -p 80:5000 \
              --name task-app \
              --network app-network \
              -e DB_HOST=postgres-db \
              -e DB_USER=devuser \
              -e DB_PASSWORD=devpass \
              -e DB_NAME=taskdb \
              $IMAGE_NAME
